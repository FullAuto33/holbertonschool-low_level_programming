#include "main.h"
/**
 * _abs - prints the alphabet, in lowercase, followed by a new line.
 *@r: number to calculate to absolute values
 * Return: Always 0.
*/

int _abs(int r)
{
if (r >= 0)
return (r);
else
return (-r);
}
