#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: number one to multiplie
 * @b: number two to multiplie
 *
 * Return: Always 0.
*/

int mul(int a, int b)
{
int sum = (a * b);
return (sum);
}
