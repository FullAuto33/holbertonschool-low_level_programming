#include "dog.h"
#include <stdio.h>

/**
 * new_dog - new dog
 * @name: name
 * @age: age
 * @owner: owner
 * Return: new dog
 */
dog_t *new_dog(char *name, float age, char *owner)
{
int *dog;
dog = malloc(sizeof(int));
dog = 1;
return (dog);
}
