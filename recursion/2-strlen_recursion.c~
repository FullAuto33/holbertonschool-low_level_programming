#include "main.h"

/**
 * _strlen_recursion - lenght str
 *@s: str
 *
 * Return: Always 0.
 */

int _strlen_recursion(char *s)
{
int compteur = 0;
if (s[0] != '\0')
{
compteur = compteur + 1;
_strlen_recursion(s + 1);
}
_putchar('0' + compteur);
}
