#include "main.h"
#include <stdio.h>

/**
 * _puts - print str
 *
 * @str : str to print
 *
 * Return: Always 0.
 */

void _puts(char *str)
{
int nbrtab = 0;
for (; str[nbrtab] != 0; nbrtab++)
{
char *str;
_putchar(*str);
}
}
