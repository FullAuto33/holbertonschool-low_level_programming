#include "main.h"


/**
 * _strcpy - copied str
 *
 *@dest : destination to str
 *
 *@src : source str
 *
 * Return: Always 0.
 */

char *_strcpy(char *dest, char *src)
{
r] = src[compteur];
compteur = compteur + 1;
}
dest[compteur] = '\0';
return (dest);
}
